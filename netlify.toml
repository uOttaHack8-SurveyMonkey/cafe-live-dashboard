[build]
  command = "npm run build"
  # QUAN TRỌNG: React App (react-scripts) xuất ra thư mục 'build', không phải '.next'
  publish = "build"

# XÓA HOẶC COMMENT ĐOẠN DƯỚI ĐÂY NẾU CÓ:
# [[plugins]]
#   package = "@netlify/plugin-nextjs"